# Pixelkiez Berlin Restaurants — Appointment Agent Package

Generated: 2026-08-26

## What this package is
An evidence-bound Pixelkiez appointment-agent campaign package for Berlin restaurants. It contains a statically validated simulation build plus reusable restaurant campaign context and behavior specifications.

## Release state
- Agent static release: STATIC_VALIDATED
- Behavioral conversation validation: NOT_EXECUTED
- ElevenLabs platform validation: NOT_EXECUTED
- Live calling authorization: NOT_AUTHORIZED
- External compliance gate: LEGAL_REVIEW_REQUIRED
- Calendar / CRM / transfer: UNVERIFIED
- Recording: OFF

## Why the included audit is synthetic
No concrete restaurant was supplied. The compiled example therefore uses a clearly synthetic restaurant and synthetic website findings. It must never be used to make claims about a real restaurant.

For every real prospect, replace the prospect/build configuration and website audit packet with validated real inputs, then rerun the release orchestrator. Any mutation invalidates the existing receipt.

## Selected simulation hooks
1. Direct reservation path visibility — selector score 0.93.
2. PDF-only menu presentation — selector score 0.8675.

## Recommended opening generated by the compiler
"Guten Tag, hier ist Pixelkiez KI-Assistent, ein KI-Call-Agent von Pixelkiez. Ich rufe wegen eines konkreten Punkts aus einem strukturierten Website-Check an. Passt es gerade für etwa 30 Sekunden, damit ich kurz sage, was wir beobachtet haben?"

## Directory map
- `inputs/` — exact build config, restaurant industry capsule, synthetic audit packet.
- `compiled/agent/` — receipt-bound compiled ElevenLabs-oriented agent assets.
- `evidence/` — build receipt and release state.
- `campaign/` — restaurant campaign blueprint, release blockers and behavior scenario specs.

## Production rule
Do not live-call from this package as-is. A real campaign needs per-lead external compliance approval, do-not-contact suppression, a validated website audit, approved meeting/tool contracts and real behavioral/platform testing.
